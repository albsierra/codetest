<div class="w-full mt-4">
    {% embed "components/ct-textarea.php.twig"
        with { 
            label: "new-exercise-form.labels.tests"|trans,
            name: '',
            exercise: exercise
        } only
        %}
        {% block input %}
            <div class="p-0 flex gap-3 items-stretch">
                <div class="w-full">
                    <div class="text-center">
                        With this inputs
                    </div>
                    <pre class="min-h-[150px]">{{ exercise.getExerciseInputTest() }}</pre>
                </div>
                <div class="flex items-center pt-6">
                    <span>
                        →
                    </span>
                </div>
                <div class="w-full">
                    <div class="text-center">
                        This are the output for you solution
                    </div>
                    <pre class="min-h-[150px] bg-white border-primary">{{ exercise.getExerciseOutputTest() }}</pre>
                </div>
            </div>
        {% endblock %}
    {% endembed %}
</div>