{% set exercise = newExercise %}
<!--    <div id="newExerciseNumber">{{ newExerciseNumber }}.</div>-->
<div class="flx-grow-all exercise-text">
    <form id="exerciseTextForm-1" action="actions/AddExercise.php" method="post">
        <label for="exerciseTitleText" class="labelForm" >{%trans%}new-exercise-form.exercisetitle{%endtrans%}</label>
        <textarea class="form-control ckeditor" id="exerciseTitleText" name="exercise[title]" rows="2" required></textarea>
        <label for="exerciseKeywords" class="labelForm" >{%trans%}new-exercise-form.keywordsseparatedlines{%endtrans%}</label>
        <textarea class="form-control" name="exercise[keywords]" id="exerciseKeywords" rows="3" placeholder="keyword1&#10;keyword2"></textarea>

        {# The tilde characther '~' is used to concatenate strings, turns operands in strings and then concatenates #}
        {{ include (main.getProperty('instructorForm')) }}

    </form>
</div>

