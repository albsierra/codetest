{% extends "mainTemplate.php.twig" %}
{% block pageTitle %}
{{ OUTPUT.pageTitle('import-authorkit.exercises.management'|trans, true, false) }}
{% endblock %}
{% block content %}
    <div>
        <div class="authorkit__projects-title">
            <div class="d-flex align-center">
                <div class="h4" style="font-weight: 500;">
                    {%trans%}import-authorkit.projects{%endtrans%}
                </div>
                <div class="ml-auto">
                    <a href="index.php" style="text-decoration: underline;">
                    {%trans%}import-authorkit.home{%endtrans%}
                    </a>
                </div>
            </div>
        </div>
        <div class="projects-list">
            {% for project in projects %}
                <a href="ak-exercises-list.php?projectId={{project.id}}">
                    {% include 'components/project-item.php.twig' %}
                </a>
            {% endfor %}
        </div>
    </div>
{% endblock %}
